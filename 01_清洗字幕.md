# 清洗后字幕稿

说明：以下内容基于你提供的抓取字幕手动清洗而成，主要修正了乱码、明显错字、重复表述和少量不通顺语句；时间戳按原字幕保留，方便和视频对照。

## 00:00 - 02:05

我回测了过去 14 年的 QQQ 历史数据，发现了一个简单、无趣，但回报却能稳定跑赢大盘的方法。这个策略就是使用 `LEAPS Call` 来抄底 `QQQ`。过去 14 年里，这套策略一共交易了 `303` 次，赢了 `300` 次，胜率高达 `99.01%`。而且它并不是一开始就满仓，资金是随着时间慢慢积累起来的，特别适合普通人使用。

这期视频我会详细讲它的操作方法：什么信号出现时可以买入，什么时候应该卖出，还有什么手段可以继续放大收益。逻辑非常简单，任何人都可以学会。

在构建量化投资系统时，最核心的第一步，是先选一个让自己能安心睡觉的底层资产。与其去赌小盘股、赌财报，不如直接买 `QQQ`。`QQQ` 追踪的是纳斯达克市值最大的 `100` 家非金融公司，代表的是科技进步的大方向。只要你相信未来十年的科技比现在更先进，那么 `QQQ` 的长期方向大概率只有一个，就是向上。

## 02:05 - 04:11

既然确定买 `QQQ`，为什么不直接买正股？因为正股太贵，资金利用率太低。对于只有几千到几万美元资金的人来说，滚雪球太慢。

所以这个策略的核心武器是 `LEAPS Call`，也就是长期看涨期权。很多人一提到期权就想到赌博，那是因为大多数人玩的是短期期权。短期期权最大的敌人是时间损耗，横盘几天，权利金可能就没了。

但 `QQQ LEAPS Call` 不一样，它有两个优势：

- 用更少的资金，撬动同样的底层上涨
- 最大亏损就是投入的权利金，不会被追加保证金，也不会爆仓

如果买 `100` 股 `QQQ` 需要大约 `6` 万美元，那么买一张两年后到期、深度实值的 `LEAPS Call`，可能只要花 `1.2` 万美元左右。同时因为是两年期合约，你有足够长的时间去等待市场反弹。

## 04:11 - 06:17

这套策略的完整回测区间是 `2012-01-01` 到 `2025-12-31`，因为 `QuantConnect` 从 `2012` 年开始才提供完整的期权链数据。

入场条件非常简单：等待市场恐慌，等 `QQQ` 的日线 `RSI` 跌破 `35`。`RSI < 35` 代表进入超卖区。只要 `RSI` 没跌破 `35`，不管市场怎么涨，都持有现金不动。只有当市场出现超卖，才买入 `QQQ LEAPS Call`。

一旦触发信号，就买入一张距离到期日约两年、`Delta` 接近 `0.7` 的看涨期权。为了防止像 `2022` 年那样的单边熊市里不断在半山腰加仓，策略增加了频率熔断机制：每周最多只允许买入一次。也就是说，如果这一整周 `RSI` 都低于 `35`，那也只在第一次触发时买一张，后面几天不再继续买。

## 06:17 - 07:22

为了衡量真实需要动用多少本金，视频里设计了一个“假想钱包”：

- 一开始给一个足够大的初始资金池，只是为了避免“有信号但买不起”
- 每次触发买点，只买一张合约
- 卖出后回收的本金和利润，全部进入“盈利钱包”
- 下次再出现买点，优先使用这个钱包里的钱
- 只有盈利钱包不够用时，才重新动用新的真实本金

这样可以精确统计，这套策略在长周期里到底实际占用了多少新增本金。

## 07:22 - 08:25

卖出规则采用阶梯止盈：

- 持仓不到 `12` 个月，如果利润达到 `100%`，无条件止盈
- 持仓在 `12` 到 `15` 个月之间，只要利润达到 `50%`，就止盈
- 持仓在 `16` 到 `18` 个月之间，只要利润达到 `30%`，就落袋为安

止损规则并不是按亏损百分比，而是按剩余期限：

- 如果期权距离到期只剩 `180` 天，不管盈亏，都必须强制平仓

也就是说，策略绝不把期权拿到最后半年，避免时间损耗急剧加速。

## 08:25 - 11:15

视频声称，按这套规则回测 `14` 年，总共交易 `303` 次，赢了 `300` 次，胜率 `99.01%`。实际动用的总本金只有 `17,247` 美元，最终资产做到 `238,593` 美元，年化收益率约 `20.64%`。

作者还做了一个对照组：把同样的 `17,247` 美元，平均分摊到整个回测期的每个交易日，做 `QQQ` 的被动定投。结果是最终约做到 `68,919` 美元，年化约 `10.4%`。作者的结论是：同样的真实投入下，`QQQ LEAPS Call` 版本把资金效率放大了。

## 11:15 - 12:18

视频还提到一个“进阶玩法”：在持有两年期 `LEAPS Call` 期间，再把它当作抵押品去卖更短期限、执行价更高的 `Covered Call`。

逻辑上相当于：

- 长期租下一套房子
- 再把它按月转租出去

这样既赚到底层 `QQQ` 上涨的钱，又能持续收到短期 `Call` 的权利金，用来摊薄 `LEAPS` 的持仓成本。视频认为，高手甚至可以在一年内把 `LEAPS` 成本降到接近零。

## 12:18 - 13:21

视频最后强调了两个现实风险。

第一，是人性考验。由于 `LEAPS` 本身带杠杆，哪怕 `QQQ` 只跌 `5%`，期权浮亏可能瞬间就到 `30%`。如果执行纪律不够强，很容易在真正反弹前被震出去。

第二，是税务影响。如果在普通应税账户里操作，这类不到 `12` 个月就平仓的盈利通常属于短期资本利得，税率可能很高。如果再叠加卖短期 `Call` 的权利金收入，税负会更重。因此视频强烈建议优先在免税账户里做这类策略。
