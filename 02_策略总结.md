# 策略总结

## 1. 一句话概括

这是一个只在 `QQQ` 大幅超卖时，分批买入两年期深度实值 `LEAPS Call`，并用阶梯止盈和剩余期限强制离场来控制风险的“抄底型长周期期权策略”。

## 2. 视频里的核心逻辑

### 底层资产

- 只做 `QQQ`
- 不碰个股
- 理由是长期方向更清晰，避免单一个股黑天鹅

### 为什么不用正股

- 正股占用资金太大
- 小资金账户复利速度慢
- `LEAPS` 可以用更少本金换取更高弹性

### 为什么是 `LEAPS`

- 时间足够长，能扛中途波动
- 最大亏损锁定在权利金
- 不需要追加保证金

## 3. 可执行规则

### 入场信号

- 指标：`QQQ` 日线 `RSI(14)`
- 条件：`RSI < 35`
- 同时要求：`VIX` 收盘值大于等于其历史扩展均值 `+ 2` 倍标准差
- 节奏：每周最多买一次

### 买什么

- 约 `2` 年到期的看涨期权
- 目标 `Delta` 约 `0.7`
- 偏深度实值
- 每次只买 `1` 张

### 资金管理

- 初始本金固定为 `$20,000`
- 中间不追加任何新资金
- 每次卖出回笼的现金继续留在账户里滚动使用
- 如果未来有信号但现金不够，本版回测会直接跳过，不会再额外注资

### 止盈规则

- 持仓 `< 12` 个月，盈利 `>= 150%`，止盈
- 持仓 `12-15` 个月，盈利 `>= 75%`，止盈
- 持仓 `16-18` 个月，盈利 `>= 30%`，止盈

### 强制离场

- 距离到期只剩 `180` 天时，不论盈亏，必须平仓

## 4. 我把视频口播转成回测口径的方式

为了能在本地脚本里回测，我做了这些明确化处理：

- 标的价格：使用 `Yahoo Finance` 的 `QQQ` 日线历史数据
- 波动率：使用 `252` 日历史实现波动率乘以 `1.2`，并限制在 `15% - 75%`
- 期权定价：使用 `Black-Scholes` 近似定价，不是逐笔真实期权链成交价
- 买入频率：严格按“每个 ISO 周最多一张”
- `VIX` 过滤：使用从 `2011` 年样本起逐日扩展计算的历史均值和标准差，只有 `VIX >= 均值 + 2σ` 才允许买入
- 到期日：固定按 `730` 天近似两年
- 风险利率：固定按 `3%`
- 账户模式：起始资金 `$20,000`，中间不追加资金

## 5. 视频里提到但我没有纳入主回测的部分

### Covered Call 进阶增强

视频提到，持有 `LEAPS` 后还可以卖短期 `Covered Call` 收权利金，进一步摊低成本。

这一段我没有纳入主回测，原因是：

- 视频没有给出短期 `Call` 的明确行权价、期限和滚动规则
- 这部分对收益影响很大，但也非常依赖人工决策
- 如果硬写进回测，主观假设会太重

所以目前回测的是“基础版”：

- 只做 `RSI < 35` 买入
- 只做阶梯止盈
- 只做到期前 `180` 天强平

## 6. 这个策略的真实优点

- 对小资金友好，资本效率高
- 只在恐慌时出手，避免长期高位追涨
- 卖出纪律比“死拿到期”更稳

## 7. 这个策略的真实弱点

- 回撤非常大，执行难度不低
- 遇到单边熊市时，连续几笔 `LEAPS` 可能一起受压
- 胜率不会像视频口播那么夸张，近似回测里会看到明显亏损单
- 如果放在普通应税账户，税务摩擦可能明显侵蚀收益
